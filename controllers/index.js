const signController = require('./signController');
const feedbackController = require('./feedbackController');
const categoryController = require('./categoryController');
const doaController = require('./doaController');
const detailController = require('./detailController');

module.exports = {
    signController,
    feedbackController,
    categoryController,
    doaController,
    detailController
}